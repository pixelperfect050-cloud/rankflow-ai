import Link from 'next/link'
import { Star, ShieldCheck, ArrowUpRight, Bookmark, GitCompare, Monitor, CheckCircle2, XCircle, ChevronRight, Share2, AlertCircle } from 'lucide-react'
import { Badge } from '@/components/ui/badge'
import { ToolCard } from '@/components/tool-card'

export default function ToolDetailPage({ params }: { params: { slug: string } }) {
  // Mock data for MVP
  const toolName = params.slug.split('-').map(w => w.charAt(0).toUpperCase() + w.slice(1)).join(' ')
  
  return (
    <main className="flex-1 bg-slate-50">
      
      {/* 1. HERO SECTION */}
      <section className="bg-white border-b border-slate-200 pt-8 pb-12">
        <div className="container mx-auto px-4 sm:px-6 lg:px-8 max-w-6xl">
          {/* Breadcrumbs */}
          <div className="flex items-center gap-2 text-sm text-slate-500 mb-8">
            <Link href="/" className="hover:text-slate-900">Home</Link>
            <ChevronRight className="w-3.5 h-3.5" />
            <Link href="/tools" className="hover:text-slate-900">Tools</Link>
            <ChevronRight className="w-3.5 h-3.5" />
            <Link href="/category/marketing-seo" className="hover:text-slate-900">Marketing & SEO</Link>
            <ChevronRight className="w-3.5 h-3.5" />
            <span className="text-slate-900 font-medium">{toolName}</span>
          </div>

          <div className="flex flex-col md:flex-row gap-8 items-start">
            {/* Logo */}
            <div className="w-24 h-24 md:w-32 md:h-32 rounded-2xl border border-slate-200 bg-white flex items-center justify-center shrink-0 shadow-md">
              <span className="text-4xl md:text-5xl font-bold text-slate-300">{toolName.charAt(0)}</span>
            </div>

            {/* Header Content */}
            <div className="flex-1 min-w-0">
              <div className="flex flex-wrap items-center gap-3 mb-2">
                <h1 className="text-3xl md:text-4xl font-bold text-slate-900">{toolName}</h1>
                <ShieldCheck className="w-6 h-6 text-sky-500" title="Verified" />
                <Badge variant="default" className="bg-emerald-50 text-emerald-700 border-emerald-200">Freemium</Badge>
              </div>
              
              <p className="text-lg text-slate-600 mb-4 max-w-3xl">
                The most advanced AI content generator that creates SEO-optimized articles, blog posts, and marketing copy in seconds. Rated #1 by marketing teams.
              </p>

              <div className="flex flex-wrap items-center gap-6 text-sm">
                <div className="flex items-center gap-1.5">
                  <div className="flex items-center text-amber-400">
                    <Star className="w-5 h-5 fill-current" />
                    <Star className="w-5 h-5 fill-current" />
                    <Star className="w-5 h-5 fill-current" />
                    <Star className="w-5 h-5 fill-current" />
                    <Star className="w-5 h-5 fill-current opacity-50" />
                  </div>
                  <span className="font-semibold text-slate-900 text-base ml-1">4.8</span>
                  <span className="text-slate-500 underline decoration-slate-300 underline-offset-4 cursor-pointer hover:text-slate-900">(342 Reviews)</span>
                </div>

                <div className="flex items-center gap-2 text-slate-600">
                  <Monitor className="w-4 h-4" /> Web, Windows, Mac
                </div>
              </div>
            </div>

            {/* Actions CTA */}
            <div className="w-full md:w-auto flex flex-col gap-3 shrink-0">
              <Link 
                href="#" 
                className="w-full md:w-64 inline-flex items-center justify-center gap-2 bg-emerald-600 hover:bg-emerald-700 text-white px-6 py-3.5 rounded-xl font-semibold shadow-lg shadow-emerald-600/20 transition-all hover:-translate-y-0.5"
              >
                Visit Website <ArrowUpRight className="w-5 h-5" />
              </Link>
              <div className="flex items-center gap-2">
                <button className="flex-1 flex items-center justify-center gap-2 bg-white border border-slate-200 hover:border-slate-300 hover:bg-slate-50 text-slate-700 px-4 py-2.5 rounded-xl font-medium transition-colors">
                  <Bookmark className="w-4 h-4" /> Save
                </button>
                <button className="flex-1 flex items-center justify-center gap-2 bg-white border border-slate-200 hover:border-slate-300 hover:bg-slate-50 text-slate-700 px-4 py-2.5 rounded-xl font-medium transition-colors">
                  <GitCompare className="w-4 h-4" /> Compare
                </button>
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Sticky Navigation */}
      <div className="sticky top-16 z-30 bg-white/80 backdrop-blur-md border-b border-slate-200 hidden md:block">
        <div className="container mx-auto px-4 sm:px-6 lg:px-8 max-w-6xl">
          <nav className="flex items-center gap-8 overflow-x-auto whitespace-nowrap">
            {['Overview', 'Pricing', 'Features', 'Pros & Cons', 'Use Cases', 'Alternatives', 'Reviews'].map((tab, i) => (
              <a key={tab} href={`#${tab.toLowerCase().replace(/ & /g, '-').replace(/ /g, '-')}`} 
                className={`py-4 text-sm font-medium border-b-2 transition-colors ${i === 0 ? 'border-emerald-500 text-emerald-600' : 'border-transparent text-slate-500 hover:text-slate-900'}`}
              >
                {tab}
              </a>
            ))}
          </nav>
        </div>
      </div>

      <div className="container mx-auto px-4 sm:px-6 lg:px-8 max-w-6xl py-12">
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-12">
          
          {/* Main Content Column */}
          <div className="lg:col-span-2 space-y-16">
            
            {/* OVERVIEW / SCREENSHOTS */}
            <section id="overview" className="scroll-mt-32">
              <h2 className="text-2xl font-bold text-slate-900 mb-6">Overview</h2>
              <div className="aspect-video w-full rounded-2xl bg-slate-200 border border-slate-200 overflow-hidden mb-6 flex items-center justify-center">
                <span className="text-slate-400">Video or Screenshot Gallery</span>
              </div>
              <div className="prose prose-slate max-w-none article-content">
                <p>
                  {toolName} is a comprehensive AI writing assistant designed to help marketers, bloggers, and SEO professionals create high-ranking content efficiently. Unlike basic AI wrappers, it uses a multi-step workflow that analyzes SERP data before generating content.
                </p>
                <p>
                  With native integrations for WordPress and Webflow, you can publish directly from the editor. It also features built-in plagiarism checking and AI detection scoring.
                </p>
              </div>
            </section>

            {/* PRICING */}
            <section id="pricing" className="scroll-mt-32">
              <h2 className="text-2xl font-bold text-slate-900 mb-6 flex items-center gap-2">
                Pricing & Plans
                <Badge variant="default" className="bg-emerald-100 text-emerald-700 ml-2">Has Free Trial</Badge>
              </h2>
              
              <div className="grid grid-cols-1 sm:grid-cols-3 gap-4">
                <div className="border border-slate-200 rounded-xl p-5 bg-white">
                  <h3 className="font-semibold text-slate-900">Free</h3>
                  <div className="mt-2 mb-4"><span className="text-2xl font-bold">$0</span><span className="text-slate-500 text-sm">/mo</span></div>
                  <ul className="space-y-2 text-sm text-slate-600">
                    <li className="flex items-start gap-2"><CheckCircle2 className="w-4 h-4 text-emerald-500 shrink-0 mt-0.5" /> 10,000 words</li>
                    <li className="flex items-start gap-2"><CheckCircle2 className="w-4 h-4 text-emerald-500 shrink-0 mt-0.5" /> Basic templates</li>
                  </ul>
                </div>
                <div className="border-2 border-emerald-500 rounded-xl p-5 bg-white relative shadow-lg">
                  <div className="absolute top-0 right-1/2 translate-x-1/2 -translate-y-1/2 bg-emerald-500 text-white text-[10px] font-bold uppercase tracking-wider px-3 py-1 rounded-full">Most Popular</div>
                  <h3 className="font-semibold text-slate-900">Pro</h3>
                  <div className="mt-2 mb-4"><span className="text-2xl font-bold">$29</span><span className="text-slate-500 text-sm">/mo</span></div>
                  <ul className="space-y-2 text-sm text-slate-600">
                    <li className="flex items-start gap-2"><CheckCircle2 className="w-4 h-4 text-emerald-500 shrink-0 mt-0.5" /> Unlimited words</li>
                    <li className="flex items-start gap-2"><CheckCircle2 className="w-4 h-4 text-emerald-500 shrink-0 mt-0.5" /> SERP Analysis</li>
                    <li className="flex items-start gap-2"><CheckCircle2 className="w-4 h-4 text-emerald-500 shrink-0 mt-0.5" /> 1-Click Publish</li>
                  </ul>
                </div>
                <div className="border border-slate-200 rounded-xl p-5 bg-white">
                  <h3 className="font-semibold text-slate-900">Team</h3>
                  <div className="mt-2 mb-4"><span className="text-2xl font-bold">$99</span><span className="text-slate-500 text-sm">/mo</span></div>
                  <ul className="space-y-2 text-sm text-slate-600">
                    <li className="flex items-start gap-2"><CheckCircle2 className="w-4 h-4 text-emerald-500 shrink-0 mt-0.5" /> 5 User Seats</li>
                    <li className="flex items-start gap-2"><CheckCircle2 className="w-4 h-4 text-emerald-500 shrink-0 mt-0.5" /> Brand Voice API</li>
                  </ul>
                </div>
              </div>
            </section>

            {/* FEATURES */}
            <section id="features" className="scroll-mt-32">
              <h2 className="text-2xl font-bold text-slate-900 mb-6">Key Features</h2>
              <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
                {[
                  'SEO Optimization Engine', 
                  'Brand Voice Customization', 
                  'Bulk Article Generation', 
                  'Plagiarism Checker',
                  'WordPress Integration',
                  'API Access'
                ].map(feature => (
                  <div key={feature} className="flex items-center gap-3 p-4 rounded-xl bg-white border border-slate-200 shadow-sm">
                    <div className="w-8 h-8 rounded-full bg-emerald-50 flex items-center justify-center shrink-0">
                      <CheckCircle2 className="w-5 h-5 text-emerald-500" />
                    </div>
                    <span className="font-medium text-slate-700">{feature}</span>
                  </div>
                ))}
              </div>
            </section>

            {/* PROS & CONS */}
            <section id="pros-cons" className="scroll-mt-32">
              <div className="grid grid-cols-1 sm:grid-cols-2 gap-8">
                <div className="bg-emerald-50/50 rounded-2xl p-6 border border-emerald-100">
                  <h3 className="text-lg font-bold text-emerald-800 mb-4 flex items-center gap-2">
                    <CheckCircle2 className="w-5 h-5 text-emerald-500" /> Pros
                  </h3>
                  <ul className="space-y-3">
                    {['Generates highly readable, human-like text', 'Excellent built-in keyword research tool', 'Clean and intuitive user interface', 'Fast generation speeds'].map((pro, i) => (
                      <li key={i} className="flex items-start gap-2 text-slate-700 text-sm">
                        <span className="text-emerald-500 font-bold">•</span> {pro}
                      </li>
                    ))}
                  </ul>
                </div>
                
                <div className="bg-red-50/50 rounded-2xl p-6 border border-red-100">
                  <h3 className="text-lg font-bold text-red-800 mb-4 flex items-center gap-2">
                    <XCircle className="w-5 h-5 text-red-500" /> Cons
                  </h3>
                  <ul className="space-y-3">
                    {['Steeper learning curve for advanced features', 'Pricing is higher than basic alternatives', 'Occasional repetition in long-form outputs'].map((con, i) => (
                      <li key={i} className="flex items-start gap-2 text-slate-700 text-sm">
                        <span className="text-red-500 font-bold">•</span> {con}
                      </li>
                    ))}
                  </ul>
                </div>
              </div>
            </section>

            {/* USE CASES ⭐ */}
            <section id="use-cases" className="scroll-mt-32">
              <h2 className="text-2xl font-bold text-slate-900 mb-2">What is it best for?</h2>
              <p className="text-slate-600 mb-6">Discover the top use cases where {toolName} excels.</p>
              
              <div className="space-y-4">
                {[
                  { title: 'Blogging & SEO', desc: 'Create 2,000+ word pillar posts that actually rank on Google by analyzing top SERP competitors automatically.' },
                  { title: 'E-commerce Descriptions', desc: 'Generate hundreds of product descriptions in bulk using CSV uploads.' },
                  { title: 'Email Marketing', desc: 'Write high-converting cold email sequences based on proven copywriting frameworks (AIDA, PAS).' }
                ].map((useCase, i) => (
                  <div key={i} className="flex items-start gap-4 p-5 rounded-xl bg-white border border-slate-200 hover:border-emerald-200 transition-colors cursor-pointer group">
                    <div className="w-10 h-10 rounded-lg bg-slate-100 group-hover:bg-emerald-100 flex items-center justify-center shrink-0 transition-colors">
                      <Star className="w-5 h-5 text-slate-500 group-hover:text-emerald-600" />
                    </div>
                    <div>
                      <h3 className="font-bold text-slate-900 mb-1">{useCase.title}</h3>
                      <p className="text-sm text-slate-600">{useCase.desc}</p>
                    </div>
                    <ChevronRight className="w-5 h-5 text-slate-300 ml-auto self-center group-hover:text-emerald-500" />
                  </div>
                ))}
              </div>
            </section>

            {/* ALTERNATIVES */}
            <section id="alternatives" className="scroll-mt-32">
              <div className="flex items-end justify-between mb-6">
                <div>
                  <h2 className="text-2xl font-bold text-slate-900 mb-1">Top Alternatives to {toolName}</h2>
                  <p className="text-slate-600">Compare {toolName} against its closest competitors.</p>
                </div>
                <Link href="/compare" className="hidden sm:flex items-center text-emerald-600 font-medium hover:text-emerald-700">
                  Compare all <ArrowUpRight className="w-4 h-4 ml-1" />
                </Link>
              </div>

              <div className="space-y-4">
                {[1, 2, 3].map((alt) => (
                  <div key={alt} className="flex items-center justify-between p-4 rounded-xl bg-white border border-slate-200 shadow-sm">
                    <div className="flex items-center gap-4">
                      <div className="w-12 h-12 rounded-lg bg-slate-100 flex items-center justify-center font-bold text-slate-400">
                        A{alt}
                      </div>
                      <div>
                        <h4 className="font-bold text-slate-900">Alternative Tool {alt}</h4>
                        <div className="flex items-center gap-2 text-xs text-slate-500">
                          <span className="flex items-center text-amber-400"><Star className="w-3 h-3 fill-current mr-0.5" /> 4.7</span>
                          <span>•</span>
                          <span>Starts at $19/mo</span>
                        </div>
                      </div>
                    </div>
                    <Link href="#" className="px-4 py-2 text-sm font-medium text-emerald-600 border border-emerald-200 rounded-lg hover:bg-emerald-50 transition-colors">
                      Compare
                    </Link>
                  </div>
                ))}
              </div>
            </section>

          </div>

          {/* Right Sidebar (Sticky) */}
          <div className="lg:col-span-1 space-y-6">
            
            {/* Tool Data Card */}
            <div className="bg-white rounded-2xl border border-slate-200 shadow-sm p-6 sticky top-32">
              <h3 className="font-bold text-lg text-slate-900 mb-4">Tool Details</h3>
              
              <ul className="space-y-4 mb-6 text-sm">
                <li className="flex justify-between items-center py-2 border-b border-slate-100">
                  <span className="text-slate-500">Starting Price</span>
                  <span className="font-medium text-slate-900">$29/mo</span>
                </li>
                <li className="flex justify-between items-center py-2 border-b border-slate-100">
                  <span className="text-slate-500">Free Trial</span>
                  <span className="font-medium text-slate-900">7 Days (No CC required)</span>
                </li>
                <li className="flex justify-between items-center py-2 border-b border-slate-100">
                  <span className="text-slate-500">Launch Date</span>
                  <span className="font-medium text-slate-900">Mar 2023</span>
                </li>
                <li className="flex justify-between items-center py-2 border-b border-slate-100">
                  <span className="text-slate-500">Has API?</span>
                  <span className="font-medium text-slate-900 flex items-center gap-1"><CheckCircle2 className="w-4 h-4 text-emerald-500" /> Yes</span>
                </li>
              </ul>

              <div className="bg-slate-50 rounded-xl p-4 border border-slate-100 mb-6">
                <h4 className="text-xs font-bold text-slate-500 uppercase tracking-wider mb-2">Integrates With</h4>
                <div className="flex flex-wrap gap-2">
                  {['WordPress', 'Webflow', 'Shopify', 'Zapier'].map(int => (
                    <span key={int} className="px-2 py-1 bg-white border border-slate-200 rounded text-xs text-slate-700">{int}</span>
                  ))}
                </div>
              </div>

              <Link 
                href="#" 
                className="w-full inline-flex items-center justify-center gap-2 bg-emerald-600 hover:bg-emerald-700 text-white px-6 py-3 rounded-xl font-semibold shadow-lg shadow-emerald-600/20 transition-all hover:-translate-y-0.5"
              >
                Try {toolName} for Free <ArrowUpRight className="w-4 h-4" />
              </Link>
              
              <div className="mt-4 flex items-center justify-center gap-1 text-xs text-slate-400">
                <AlertCircle className="w-3 h-3" /> We may earn a commission
              </div>
            </div>

            {/* Related Articles */}
            <div className="bg-white rounded-2xl border border-slate-200 shadow-sm p-6">
              <h3 className="font-bold text-lg text-slate-900 mb-4">Related Articles</h3>
              <div className="space-y-4">
                {[
                  '10 Best AI Writers for SEO in 2024',
                  'How to scale your blog with AI',
                  `${toolName} vs ChatGPT: Which is better?`
                ].map((article, i) => (
                  <Link key={i} href="#" className="block group">
                    <h4 className="text-sm font-medium text-slate-700 group-hover:text-emerald-600 transition-colors line-clamp-2">{article}</h4>
                  </Link>
                ))}
              </div>
            </div>

          </div>
        </div>
      </div>
    </main>
  )
}
